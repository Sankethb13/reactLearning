## Swiggy API issue

- Whenever there is a change in the response o

## CORS issue resolved.

- CORS.io is the alternative for CORS module in the chrome extentions
  CORS : This is to communicate with the outside environment. That is from local host to the other env
  So browser blocks this communication. Hence we use CORS extention to resolve this issue. Apart from the extention we use this header before the URL.
